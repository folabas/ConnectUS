# Base URL of the ConnectUs API. Used for both HTTP and the socket connection.
NEXT_PUBLIC_API_URL=http://localhost:5000

# Optional: Mux environment key, for playback analytics on uploaded films.
NEXT_PUBLIC_MUX_ENV_KEY=
